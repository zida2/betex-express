rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Règles de base : accès complet pour le développement
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
